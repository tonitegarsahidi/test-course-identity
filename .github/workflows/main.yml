name: Deploy Lambda Identity

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.x]
    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm ci
    - name: serverless deploy
      uses: serverless/github-action@v3.2
      with:
        args: deploy
      env:
        SERVERLESS_ACCESS_KEY: ${{ secrets.SERVERLESS_ACCESS_KEY }}
        # or if using AWS credentials directly
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        HOST: localhost
        PORT: 8081
        DB_HOST: testcoursedev.cyoiqruurdqt.eu-north-1.rds.amazonaws.com
        DB_USER: testcoursedev
        DB_PASSWORD: Duni4SementaraAkhiratSelamany4
        DB_NAME: testcoursedev
        DB_PORT: 3306
        DB_PASSWORD_PEPPER: l4osJah3danKetumb@rrrr
        JWT_SECRET: 521398c85df1391a7400600c465dbb86aa84d446ae017e255005d6b295f51c84